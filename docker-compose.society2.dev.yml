services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: resolute_society_2
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "5434:5432"

  app:
    build: .
    ports:
      - "3002:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      ORIGIN: http://localhost:3002
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD:-postgres}@postgres:5432/resolute_society_2
      DATA_DIR: /app/data
      SOCIETY_PUBLIC_URL: host.docker.internal:3002
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - app-data:/app/data
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres-data:
  app-data:
